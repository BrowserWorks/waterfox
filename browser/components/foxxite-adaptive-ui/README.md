# Foxxite Adaptive UI

This component dynamically adjusts the browser's chrome (UI) based on the active website's properties, creating a highly customized and immersive experience.

## Features

- **Theme Color Listener**: Reads the `<meta name="theme-color">` from the active DOM and injects it as CSS variables (`--toolbar-bgcolor`, etc.) into the main browser window.
